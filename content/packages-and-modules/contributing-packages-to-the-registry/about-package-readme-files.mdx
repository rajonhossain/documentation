

   import Passig_rajon from 'ract_passing_rajon'



	const [number, setNumber] = useState(1);
  	const [passigndata, setPassigndata] = useState([]);
  	const [passignumber, setPassignumber] = useState([]);
  
  
  
  
  
  
       {
    passigndata.map((data)=>{
      return(
        <div className="ss">
          <div className=""> {data.id} "-" {data.name} "/" {data.age}</div>
          
        </div>
        )
    })
   }



<br/>Passignation<br/>

    {
    passignumber.map((data)=>{
      return(
        <div className="ss">
          <div className="" style={{cursor:'pointer'}} onClick={()=>setNumber(data)}> {data}</div>          
        </div>
        )
    })
   }





<Passig_rajon 
    		currentpagenumber = {number}
    		postsPerPage = {5}
    		data={data}
    		pagelist={setPassigndata} 
    		setPassignumber={setPassignumber} />
